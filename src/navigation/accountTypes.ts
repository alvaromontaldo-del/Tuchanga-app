/**
 * Tipos de la pila “Perfil” (tab Perfil).
 * Los `ParamList` viven en `mainTypes.ts` para evitar dependencias circulares.
 */
export type {
  AccountStackParamList,
  AccountStackScreenProps,
} from './mainTypes';
